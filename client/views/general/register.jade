h1 Register
.row
  .col-lg-3.col-md-2.col-xs-1
  .col-lg-6.col-md-8.col-xs-10
    form.form-signin(name='todoForm', novalidate)
      .form-group(ng-class="{ 'has-error' : todoForm.email.$invalid }")
        label Email
        input.form-control(type='email', name='email', ng-model='user.email')
        p.help-block(ng-show='signinForm.email.$invalid') Enter a valid email.
        .form-group(ng-class="{ 'has-error' : regForm.password.$invalid }")
          label Password
          input.form-control(type='password', name='password', ng-model='user.password', ng-minlength='8')
          p.help-block(ng-show='todoForm.password.$error.minlength') Min password length is 8 characters.
          p(style='color:red;', ng-show='todoError') {{regErrorMessage}}
        button.btn.btn-lg.btn-primary.btn-block(type='button', ng-click='signUp();', ng-disabled='!user.email || !user.password') Register

  .col-lg-3.col-md-2.col-xs-1 
  
  
